/**
 * 与自选合并拉取的默认行情代码（仅当自选为空时由 MainPanel 并入拉取）。
 * 上证/深证/沪深300/创业板等主要指数已在底部 **MarketRibbon** 展示，勿再放进此处。
 * 保留一只常见 ETF 作为「零自选」时的示例标的；一旦有自选则不再默认拉取，避免误当成自选却删不掉。
 */
export const DEFAULT_QUOTE_CODES = ["sh510300"];
